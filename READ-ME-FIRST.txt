KOVELINE — PAPER I v3

If you installed the FIRST Paper I version, delete these — they are gone now:
    app/paper-1/            (whole folder)
    components/paper-one.tsx
    content/paper-1.json
and revert the Explore row for "/paper-1" in components/navbar.tsx and the
"/paper-1" line in app/sitemap.ts. Both are handled automatically now.

IMPORTANT: paper-1 goes in content/islam/, NOT content/.
Correct: content/islam/paper-1/
Wrong:   content/paper-1/      <- this is what caused the subject.json error

Drag these into your koveline folder and Merge/Replace:

    app/[subject]/[course]/page.tsx   <- the paper index
    app/globals.css
    components/deck/scan.tsx          <- NEW
    components/deck/rich-body.tsx
    components/deck/engine.tsx
    components/paper-list.tsx         <- NEW
    components/home.tsx
    lib/content/schema.ts
    content/islam/paper-1/
    public/paper-1/
    NOTES.md

Then:  npm run validate   (expect 1234 items, 0 errors)
       npm run build

WHAT CHANGED IN v3
  - Papers run newest first: 2025, 2024, 2023, 2022, 2021, 2020 Specimen
  - New paper index at /islam/paper-1 with sorting
    (Newest first / Oldest first / Most questions)
    The home page card now opens this instead of jumping into the mixed deck.
  - The zoom viewer opens scrolled to the RIGHT edge, since Dhivehi starts
    there — you no longer land on the end of the line.
  - Syllabus courses (grade-9, grade-10) still redirect to the home page
    exactly as before; only paper courses get an index.


KOVELINE — REVISION BANK UPDATE

Added in this working copy:
  - Paper II complete bank: 150 cards, 2020 Specimen + 2021–2025
  - Paper II scenarios: 36 focused Q20–25 cards using the same source scans
  - Deyha Grade 9: 60 cards across Sections 1–3
  - Deyha Grade 10: 86 cards across Sections 1–3

All four scan-based study areas (Paper I plus the three additions) use the
normal DeckEngine: In order / Random, Review wrong, question navigator,
keyboard shortcuts, mobile action bar, completion state, and account-scoped
progress. Paper II and Deyha source fragments are preserved as WebP scans;
multi-part source crops are stitched vertically without resampling so the
standard Paper I scan viewer can be reused.
